{
  "name": "axona-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "Axona Phase 2 — WebSocket bridge: ping/pong + WebRTC signaling relay for the peer mesh",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node scripts/smoke-client.js",
    "smoke:signal": "node scripts/signal-smoke.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "license": "MIT"
}
